<iframe src="https://giphy.com/embed/ToMjGplHPoYqSg7oACc" width="480" height="480" frameBorder="0" class="giphy-embed" allowFullScreen></iframe><p><a href="https://giphy.com/gifs/bleedgfx-burger-ToMjGplHPoYqSg7oACc"></a></p>
<h1>I only have eyes for burgers and fries</h1>

<h2>On the menu</h2>

<ol>
  {{#each burgers}}
    {{#unless devoured}}
      <li>
	    {{burger_name}}
        <button class="devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		    {{#if devoured}}Devour It!{{/if}}
	    </button>
    </li>
    {{/unless}}
  {{/each}}
</ol>

<h3>In my belly</h3>

<ol>
  {{#each burgers}}
    {{#if devoured}}
    <li>
        {{burger_name}}
    </li>
    {{/if}}
  {{/each}}
</ol>

<h2>What's new on the menu?</h2>
<form class="new-burger">

  <div class="form-info">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <button type="submit">Add Burger</button>
</form>